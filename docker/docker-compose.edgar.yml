services:
  edgar-webserver:
    extends:
      file: docker/docker-compose.yml
      service: webserver
    container_name: arelle-edgar-webserver
    build:
      args:
        INCLUDE_EDGAR: "true"
        # EXTRA_PIP: "gunicorn"
        # or
        # EXTRA_PIP: "-r requirements-extra.txt"
