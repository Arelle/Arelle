---
title: Legacy EDGAR pages
build:
  render: never
  list: never
---

These pages exist for one reason: the SEC linked to them from its own documentation. They are compatibility surfaces, not directly discoverable from arelle.org.

Each page is pinned to the exact URL WordPress served it at, via `url:` front matter.
